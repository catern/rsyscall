"""Various process implementations with various different special abilities.

Note that all process types can launch any other process type, in arbitrary combinations.

Note also that users are able to implement their own types of processs,
so if these available process types do not meet your needs,
you can implement a new kind of process which does.

"""
